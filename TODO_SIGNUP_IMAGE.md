# TODO: SignUp Image Picker Implementation

## Steps
1. [x] Edit `lib/viewmodels/signup_viewmodel.dart` — add `setSelectedImage(XFile? image)` public method to sync local image pick with ViewModel.
2. [x] Edit `lib/screens/signup_screen.dart` — implement full image picker UI logic (clean imports, state variable, bottom sheet, GestureDetector, conditional image display, sync with ViewModel).

